# HRM Overview (L0–L2)

This module introduces hierarchical residual memory levels on top of the Coherence Engine.

- L0: fused sensor features
- L1: short-lived motifs
- L2: stable concepts

See `hrm_api.md` for integration details and `adapters/hrm_bridge.py` to wire into the engine.
